/*
take coordinates from overhead and travel to the mission zone
Here are the steps:
1. there are two possible landing points (x1,y1) or (x2,y2) (coordinates wont be exact 
   allow a buffer) find the coordinates and determine the coordinates of the mission zone
2. code a method to decide which translation function to implement 
   

*/
#include <translation.ino>
//also need to include the enes100 library and the wifi module library 
